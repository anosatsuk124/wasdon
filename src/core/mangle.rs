use ::alloc::format;
use ::alloc::string::String;

/// Mangle `str` with the mangling rules.
pub fn mangle_str(str: Option<String>, rule: ManglingRule) -> String {
    unimplemented!("Will be deprecated soon")
}

pub enum ManglingRule {}
